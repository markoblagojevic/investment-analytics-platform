services:
  mysql:
    image: mysql:8.4
    container_name: investment-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: investment_analytics
    ports:
      - "3307:3306"
    volumes:
      - investment_mysql_data:/var/lib/mysql

volumes:
  investment_mysql_data: